framework:
    mailer:
        dsn: '%env(MAILER_DSN)%'
        # Emails are queued and sent via Messenger (delayed fallback)
        message_bus: messenger.default_bus

when@test:
    framework:
        mailer:
            dsn: 'null://null'
