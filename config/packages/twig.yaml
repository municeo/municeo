twig:
    default_path: '%kernel.project_dir%/templates'
    # Fail fast on undefined variables — forces explicit template contracts
    strict_variables: true

when@test:
    twig:
        strict_variables: true
