import { startStimulusApp } from '@symfony/stimulus-bundle';

const app = startStimulusApp();

// Register application Stimulus controllers here as they are created, e.g.:
// import GpsController from './controllers/gps_controller.js';
// app.register('gps', GpsController);

export { app };
